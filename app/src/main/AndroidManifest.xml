<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Required to allow the app to send events and user profile information -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Recommended so that CleverTap knows when to attempt a network call -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" /> <!-- Optional for push notifications -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".CTapp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/premium"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/premium"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test_push"
        tools:targetApi="31">
        <activity
            android:name=".WebViewActivity"
            android:exported="false" />
        <activity
            android:name=".InApp_Templates"
            android:exported="false" />
        <activity
            android:name=".PushTemplatesActivity"
            android:exported="false" />
        <activity
            android:name=".CustomInbox"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EventsActivity"
            android:label="Events"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".LogActivity"
            android:label="Activity Logs"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".SecondActivity"
            android:exported="true" />

        <meta-data
            android:name="CLEVERTAP_ACCOUNT_ID"
            android:value="58Z-5W6-866Z-PRANJALAPP2" />
        <meta-data
            android:name="CLEVERTAP_TOKEN"
            android:value="605-1b5" />
        <meta-data
            android:name="CLEVERTAP_REGION"
            android:value="eu1" />
        <meta-data
            android:name="CLEVERTAP_USE_GOOGLE_AD_ID"
            android:value="0" />
        <meta-data
            android:name="CLEVERTAP_NOTIFICATION_ICON"
            android:value="small_icon" /> <!-- Use your icon's filename without the extension -->
        <meta-data
            android:name="CLEVERTAP_ENCRYPTION_LEVEL"
            android:value="1" />

        <service
            android:name="com.clevertap.android.sdk.pushnotification.fcm.FcmMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name="com.clevertap.android.sdk.pushnotification.CTNotificationIntentService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.clevertap.PUSH_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>